import { Project } from "@/types/project";

type ProjectContributionProps = {
  project: Project;
};

export default function ProjectContribution({
  project,
}: ProjectContributionProps) {
  return <section>ProjectContribution</section>;
}
